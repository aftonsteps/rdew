#' @keywords internal
"_PACKAGE"

# The following block is used by usethis to automatically manage
# roxygen namespace tags. Modify with care!
## usethis namespace: start
## usethis namespace: end
## Silence the package checking note "no visible binding for global variable..."
global_vars <- c(".data", ":=", "object_id", "purchased_price_joja_no_membership", "purchased_price_joja_with_membership", "purchased_price_pierre", "row_number", "sell_price", "type", "vals")

globalVariables(global_vars)
